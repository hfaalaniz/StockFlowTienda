[build]
  publish = "dist"
  command = "node scripts/prepare-netlify-env.mjs && npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit --no-fund"

# SPA fallback: rutas desconocidas → index.html (necesario para React Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
